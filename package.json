{
  "private": true,
  "name": "notes",
  "engines": {
    "node": ">=18.0.0"
  },
  "workspaces": ["backend", "frontend", "frontend-server", "nginx", "notes-db", "pgadmin4", "task-manager"],
  "scripts": {
    "start": "docker compose -f docker-compose.dev.yml rm -fsv && docker compose -f docker-compose.dev.yml up --build",
    "start:prod": "docker compose -f docker-compose.prod.yml up --build",
    "check": "lerna run check"
  },
  "devDependencies": {
    "lerna": "^8.1.2"
  }
}
